{
  "name": "finora-root",
  "private": true,
  "workspaces": [
    "finora"
  ],
  "scripts": {
    "postinstall": "npm --prefix finora install",
    "dev": "npm --prefix finora run dev",
    "build": "npm --prefix finora run build",
    "preview": "npm --prefix finora run preview"
  },
  "dependencies": {
    "firebase": "^12.19.0"
  }
}
